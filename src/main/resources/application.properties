# El fix del modo headless (para que el JFrame de la tira LED pueda abrir)
# NO va aqui: spring.main.headless=false no funciona porque Spring Boot fija
# java.awt.headless antes de leer este fichero. El fix real esta en
# CrewChiefApplication.main(), con System.setProperty(...) antes de
# SpringApplication.run().
